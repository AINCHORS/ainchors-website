@extends('layouts.admin')

@section('title', $lead->full_name.' | Contact Submission | AINCHORS Admin')

@section('content')
    <div class="mx-auto max-w-4xl"><a href="{{ route('admin.leads.index') }}" class="inline-flex items-center gap-2 text-sm font-semibold text-ainchors-green transition hover:text-ainchors-navy focus:outline-none focus:ring-2 focus:ring-ainchors-green"><svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m15 19-7-7 7-7"/></svg>All contact submissions</a><div class="mt-5 rounded-ainchors-card border border-ainchors-navy/10 bg-white p-6 shadow-sm sm:p-8"><div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[0.16em] text-ainchors-green">CRM</p><h1 class="mt-2 font-heading text-3xl font-bold text-ainchors-navy">{{ $lead->full_name }}</h1><p class="mt-2 text-sm text-ainchors-grey-dark">Received {{ $lead->created_at?->format('j M Y, H:i') ?? '—' }}</p></div>@include('admin.partials.status-badge', ['status' => $lead->status])</div><div class="mt-7 grid gap-6 lg:grid-cols-[minmax(0,1fr)_minmax(17rem,0.75fr)]"><section aria-labelledby="lead-contact-heading"><h2 id="lead-contact-heading" class="font-heading text-2xl font-bold text-ainchors-navy">Contact details</h2><dl class="mt-4 divide-y divide-ainchors-navy/8 text-sm"><div class="flex justify-between gap-5 py-3"><dt class="font-semibold text-ainchors-grey-dark">Email</dt><dd class="max-w-[15rem] break-words text-right text-ainchors-navy">{{ $lead->email }}</dd></div><div class="flex justify-between gap-5 py-3"><dt class="font-semibold text-ainchors-grey-dark">Phone</dt><dd class="text-right text-ainchors-navy">{{ $lead->phone ?: '—' }}</dd></div><div class="flex justify-between gap-5 py-3"><dt class="font-semibold text-ainchors-grey-dark">Company</dt><dd class="text-right text-ainchors-navy">{{ $lead->company_name ?: '—' }}</dd></div><div class="flex justify-between gap-5 py-3"><dt class="font-semibold text-ainchors-grey-dark">Source</dt><dd class="text-right text-ainchors-navy">{{ str($lead->source)->replace('_', ' ')->headline() }}</dd></div></dl></section><section aria-labelledby="lead-status-heading" class="rounded-ainchors-button bg-slate-50 p-5"><h2 id="lead-status-heading" class="font-heading text-xl font-bold text-ainchors-navy">Relationship status</h2><p class="mt-2 text-sm leading-relaxed text-ainchors-grey-dark">Update the stage without deleting the original contact submission.</p><form method="POST" action="{{ route('admin.leads.update', $lead) }}" class="mt-5 space-y-4">@csrf @method('PUT')<label for="lead-show-status" class="block text-sm font-semibold text-ainchors-navy">Status</label><select id="lead-show-status" name="status" class="block w-full rounded-ainchors-button border border-ainchors-grey-light/45 bg-white px-3.5 py-2.5 text-sm text-ainchors-navy focus:border-ainchors-green focus:ring-2 focus:ring-ainchors-green/25">@foreach (['new', 'contacted', 'qualified', 'consultation_requested', 'consultation_booked', 'proposal', 'won', 'lost'] as $status)<option value="{{ $status }}" @selected($lead->status === $status)>{{ str($status)->replace('_', ' ')->headline() }}</option>@endforeach</select><button type="submit" class="rounded-ainchors-button bg-ainchors-green px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-ainchors-navy focus:outline-none focus:ring-2 focus:ring-ainchors-green focus:ring-offset-2">Save status</button></form></section></div>@if (filled($lead->notes))<section aria-labelledby="lead-notes-heading" class="mt-6 border-t border-ainchors-navy/10 pt-6"><h2 id="lead-notes-heading" class="font-heading text-2xl font-bold text-ainchors-navy">Notes</h2><p class="mt-3 whitespace-pre-line text-sm leading-relaxed text-ainchors-grey-dark">{{ $lead->notes }}</p></section>@endif</div></div>
@endsection
